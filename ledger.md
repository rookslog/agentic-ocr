# Decision-observability ledger

Append-only. Per PLAN.md §8 item 4: *every significant pipeline change logs
predicted impact before, verdict after.* Rows are immutable once written — never
edit a past row; append a new one (with a back-reference) to correct or update it.

Verdict vocabulary (PLAN §8 item 1): `Corroborated` · `Reported` · `Resolved` ·
`Concordant` · `Survived current tests` · `Partially supported` · `Proxy support` ·
`Underdetermined` · `Not tested` · `Normative judgment`.

| # | Date | Change | Prediction (before) | Verdict (after) | Provenance |
|---|------|--------|---------------------|-----------------|------------|
| 1 | 2026-06-12 | Scaffold the `agentic-ocr` repo: port `eval/lib` scoring core from scholardoc `ground_truth/lib`, set up uv + pytest + ruff + mypy + CI, seed experiments E1–E7 preregs, runner skeleton, process files. | Ported metrics harness runs unchanged after import rebase (`ground_truth.lib` → `eval.lib`); the only code that breaks is the `scholardoc.models`-dependent adapter, which is deferred. CI lint/typecheck/test go green; no PDFs or >1MB blobs enter history. | **Survived current tests** — 44/44 ported unit tests pass; ruff + mypy clean; size/PDF guard finds nothing. `scholar_doc_to_elements` deferred as predicted (untested, `# pragma: no cover`). One scholardoc test left behind (regression test needs the rotted pipeline). | Commit `d4fd205` (PLAN/LICENSE/.gitignore) + scaffolding commit; `eval/lib/` ported from `./scholardoc@revival/2026-05-audit-and-reset`. |
| 2 | 2026-06-12 | Add the delegation-triage + credit-assignment layer (`docs/delegation-triage.md`, `delegation-log.jsonl`), the first two /goal packets (`goal/`), and the upstream-feedback record (`docs/process/upstream-feedback.md`). | By the Phase-0 gate meta-audit: the log holds every delegation made (no untraced delegations); ≥1 rubric calibration is detected from real entries (overshoot, undershoot, or token-prediction bias — D-001 already shows a 2× token overestimate); the credit-assignment edge gets exercised by ≥1 review-verdict event at a T3+ gate. Disconfirmer: if rows are missing for delegations that happened, or no review-verdict ever lands, the layer is unenforced ceremony — cost without benefit (X-003) — and gets cut or mechanized, not kept as decoration. | **Not tested** — written before any gate has run. | Session 2ec3dc81; D-001/D-002 bootstrap entries; prior-art survey `.local/research/observability-prior-art.md` (local-only). |
| 3 | 2026-06-12 | Adversarial review of PR #2 (D-005 failure-modes / opus-max; D-006 citation / opus-xhigh) and response. Hardened the validator: computed-delta guardrail (closes the `overfit_steps`-understatement bypass, F4a); `diagnosis_outcome` field makes `diagnosis_hit_rate` computable (F2); split `undershoot_rework_rate` into `rework_rate` + counterfactual `undershoot_rate` (F4/F5). Fixed dangling `PLAN §5.3`→`§5` (F15) and packet M3 escalation (F7). | Both reviewers returned APPROVE-WITH-CHANGES with no fatal design hole; the credit-assignment edge is now exercised by D-001/D-002 `review-verdict` rows (`review_linkage_fraction` > 0). **Corrects row 2** (append, not edit): "no untraced delegations" is not checkable from the log alone (F1) — covers *traced* delegations only until an out-of-band transcript diff exists. Deferred F3 / F4b / F6 / F1 tracked as intervention I-001 with pre-registered signals. | **Partially supported** — design survived adversarial review with fixes applied; meta-audit stats now computable but not yet swept at a gate. | Reviews D-005/D-006 (transcripts); this PR. |
