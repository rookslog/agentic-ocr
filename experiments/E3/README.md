# E3 — Cascade dividend (A1 vs A0)

**Status:** prereg pending · run order: after E2.

**Hypothesis (one line).** Confidence-routed escalation + consensus voting on
divergent regions (C1) recovers **≥70% of the frontier-quality gap at ≤3× B\* cost**.

**Pre-registered disconfirmer.** ΔQuality ≤ 0 **or** cost > the pre-set ceiling ⇒
the cascade is *cost without benefit* at this corpus mix (the X-003 rule).

Also tests the routing-signal ablation: checkers + cross-engine agreement vs model
self-confidence — prediction is that **self-confidence routing is worse** than
checker routing (PLAN §6 mechanism 4).

Full pre-registration → `prereg.md`. Derived from PLAN.md §9 E3.
